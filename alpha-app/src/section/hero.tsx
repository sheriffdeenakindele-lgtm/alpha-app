import react from 'react';
import Wrapper from '../shared/wrapper';

const Hero = () => {
    return (
        <section>
            <Wrapper>
                <div>

                </div>
            </Wrapper>
        </section>
    );
};

export default Hero;
